__pycache__/
*.py[cod]
.pytest_cache/
.claude/settings.local.json
.env
.env.*
!.env.example
.venv/
venv/
*.log
.DS_Store
Thumbs.db
# Word/WPS 打开文档时留的锁文件（~$xxx.docx），不是内容
~$*

# Local H5 note data and user uploads
data/notes.json
data/cards.json
data/review_records.json
uploads/

# 账号：凭证索引（含密码 hash）、每账号的目录（含资料）、token 签名密钥。
# 这三样都是本机跑出来的运行时状态，绝不能进版本库。
# data/corpus/example.jsonl 是语料库，必须留在库里，不要写成整目录忽略。
data/users.json
data/users/
data/.secret_key

# CLI 演示缓存与出图（precompute / make_figures 生成）——本机产物，不进库。
# ⚠️ 这两条必须带前导斜杠（锚定到仓库根）：写成 assets/ 会把前端的 web/assets/
# 站点图标一起忽略掉，那目录是要进镜像的。
/data/demo_cache.json
/assets/

# macOS 压缩包解出来的残留：__MACOSX/ 目录与 ._ 前缀的资源分叉文件，都不是内容。
._*
__MACOSX/

# 出图/出文档脚本的产物（build_product_pdf.py、build_deploy_doc.py），
# 本机重跑即可复现，不进库。
/output/
/daily-login-08.png/

# ⚠️ 这份 docx 里手打进了真实的 LC_API_KEY（.env 已在上面忽略，这份是同一把）。
# 要拿它进库，先把密钥换回占位符「sk-交付方私聊发给你的那串」再提交。
/CloudBase演示部署方案.docx

# 演示录屏的中间产物：原始 webm 走 Release 分发，切片目录和路径指针是本机状态。
# 成片 demo/logic_coloc_demo.mp4 要进库，所以别图省事忽略整个 demo/。
/demo/segs/
/demo/raw.video.path
/demo/*.webm
